TASK :- 
open camera
start video streaming,
start recording, 
click img,
store that img ||  vdo in laptop , 
open file.  play video, pause vdo,progress bar,drag the vdo,
total vdo duration 

ALL APIs USED :-
naviagator.mediadevices.getUserMedia()
mediaStreaming()
mediaRecorder()
new Blob[]
a.href,a.download(),
e.target.files[0],createObjectUrl(fileName),<vdo ref={url}>, 
vdo.play(),pause(),vdo.currentDuration,vdo.totalDuration(),
<input   type=range max={totalDuration} min={0}>==>progressBar,
vdo.current=user input number=====drag vdo ,

//17Sept2024-camera-img-downLoad
